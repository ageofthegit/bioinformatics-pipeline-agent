# Workings — 11 August 2026

## 1. Phase 4 baseline

- Re-ran the Phase 3 baseline and confirmed all 16 existing tests passed.
- Reviewed the monitoring and controlled-recovery acceptance criteria.

## 2. Job monitoring

- Added timestamped `queued`, `running`, `completed`, `failed`, and `awaiting_retry_approval` states.
- Preserved command, runner version, output, errors, exit code, duration, and each execution attempt in saved state and audit history.
- Added deterministic plain-language summaries for known runner, permission, process, and missing-output failures.

## 3. Human-approved recovery

- Added one proposed retry using the unchanged approved input, threshold, runner, and resources.
- Required a separate recorded approval; rejection stops the run, and a second failure cannot retry again.
- Ensured failed runs cannot create or accept a report.

## 4. Verification and next step

- Verified 21 passing tests, including success, failure, rejected retry, approved retry, and the one-retry limit.
- Rechecked Python/Nextflow equivalence and completed a real Nextflow 26.04.6 demonstration with `queued → running → completed` history.
- Marked Phase 4 complete. Phase 5 is a small local MCP interface over the existing controlled tools.

## 5. Phase 5 — controlled MCP tools

- Reviewed the current official MCP Python SDK documentation and pinned stable SDK 2.0.0 in a project-only `.venv`.
- Added five typed local `stdio` tools: FASTQ validation, plan proposal, pending run request, status lookup, and saved-result summary.
- Restricted MCP reads to `.fastq`/`.fq` files inside `data/`; rejected unsafe paths, unsupported files, and unknown IDs.
- Made pipeline requests write an audited `awaiting_human_approval` record without executing anything.
- Added a separate person-operated command that displays the existing plan and approval prompt.
- Verified 34 tests through the real in-memory MCP protocol. A manual rejection demonstration created no execution attempt and linked both audit records.
- Marked Phase 5 complete. Phase 6 is a narrow, optional LLM explanation layer.

## 6. Phase 6 — grounded explanation layer

- Added a provider-neutral interface and an offline deterministic demo provider; no external service, API key, or paid call is used.
- Limited plan explanations to runner, validated read count, quality threshold, and fixed step count.
- Limited result explanations to saved QC measurements and kept all validation, calculations, execution, retries, and approvals outside the provider.
- Labelled explanations as separate from measurements and approval decisions.
- Rejected invented numbers, workflow-control instructions, sensitive-data requests, unsupported clinical claims, and injected filename/metadata text.
- Verified the full offline demonstration and expanded the suite to 44 passing tests.
- Marked Phase 6 complete offline. Phase 7 is a repeatable evaluation suite.

## 7. User verification and learning

- The project owner activated `.venv`, approved the Phase 6 plan, reviewed the report, and completed run `run-20260811-045726-350327` with status `accepted_by_human`.
- Created a 22-question interactive Phase 5–6 quiz covering MCP tools, approval boundaries, path safety, grounded explanations, prompt injection, and responsibility separation.

## 8. Phase 7 — repeatable evaluation

- Added versioned cases for four valid FASTQ scenarios, three malformed inputs, four adversarial explanations, and the five-tool MCP contract.
- Added one command that checks validation, QC maths, recommendations, report consistency, approval enforcement, prompt-injection resistance, and MCP behaviour.
- Made failed checks return a non-zero exit code and kept subjective explanation quality separate for human review.
- Generated a dated report with known limitations: 26 evaluation checks passed and 47 unit tests passed in `.venv`.
- Marked Phase 7 complete. Phase 8 requires approval before downloading a small public genomic dataset.

## 9. Phase 8 — public genomic data

- Selected approved ENA run `DRR002057`: public, non-human *E. coli* whole-genome reads; 14.2 MB compressed.
- Added a versioned provenance manifest and downloader with byte-size, MD5, decompression, and FASTQ record-count checks; downloaded reads remain ignored by Git.
- Verified the published MD5 and 128,518 reads, then ran the unchanged workflow in automatic demonstration mode.
- The agent warned that 87.99% of reads were below Phred 20 and did not confuse successful execution with acceptable data.
- Matched ENA's read count and an independent 38,828,067-base count; Python and Nextflow results were identical.
- Recorded that ENA's run-level base count differs from its generated FASTQ instead of presenting it as a match.
- Expanded the suite to 51 passing tests. Phase 9 is a local compute-queue simulation.

## 10. Phase 8 human review

- The project owner approved the real-data analysis plan for run `run-20260811-062413-160285`.
- Execution completed successfully, but the report warned that 87.99% of reads were below Phred 20 and read lengths varied substantially.
- The project owner did not accept the final report; the audited status is correctly `needs_human_review` rather than accepted or failed.
- Further review concerns the generated report and data suitability; manually reading the full 82.6 MB FASTQ is not required.

## 11. Phase 9 — local compute queue

- Added a local queue wrapper for either Python or Nextflow; direct execution remains available.
- Recorded CPU, memory, wall time, executor, queue job ID, status history, and local cost as `not_applicable`.
- Set defaults of 1 CPU, 1,024 MB, and 600 seconds; increases require a separate approval.
- Rejected and audited requests above 4 CPUs, 4,096 MB, or 3,600 seconds before execution.
- Added wall-time failure guidance and tests for default, increased, rejected, and over-limit requests.
- Verified 60 passing tests, successful queued Python and Nextflow demonstrations, and an audited 5-CPU rejection with no execution.
- No cloud or HPC service was created. Phase 10 is full provenance and reproducibility.

## 12. Phase 10 — provenance and reproduction

- Added `manifest.json` to every terminal run path with SHA-256 fingerprints for the input, source tree, and Nextflow pipeline.
- Recorded invocation, parameters, runner/executor, resources, software and dependency versions, timestamps, execution details, results, and approval events.
- Added `python -m bioagent.reproduce run-...`; it refuses missing or changed input and always asks for fresh plan and report approvals.
- Verified changed-input detection, rejected-run manifests, approval enforcement, and equivalent reproduced sample results in six new tests.
- Verified 66 total tests, 26 evaluation checks, MCP integration, and Python/Nextflow equivalence.
- Created automatic demonstration run `run-20260811-070041-316920`; its manifest correctly labels both approvals as `automatic_demo`.
- Marked Phase 10 complete. Phase 11 is a small user-owned pilot.

## 13. Additional public datasets

- Selected three public, non-human ENA runs and excluded human-host-associated candidates.
- Added versioned manifests for yeast `ERR1229325`, environmental *Pseudomonas* `SRR29651967`, and laboratory *E. coli* `SRR12966849`.
- Preserved the visible WGS/16S title mismatch in ENA's `SRR29651967` metadata.
- Downloaded and extracted all three; official file sizes, MD5 checksums, FASTQ structure, and read counts matched.
- Added accession-aware download and independent-check commands for testing one dataset at a time.
- Expanded the suite to 68 passing tests. No analysis result or human acceptance has been recorded for these three datasets yet.
